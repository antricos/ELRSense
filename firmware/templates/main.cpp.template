/**
 * AUTO-GENERATED by the ELRSense configurator.
 * Board: {{BOARD_NAME}}
 * Sensors: {{SENSOR_LIST}}
 *
 * Regenerate via the configurator rather than hand-editing if you need to
 * change the board or sensor selection -- this file is assembled from
 * firmware/templates/main.cpp.template plus the chosen board/sensor
 * sources, each of which exposes a matching <sensor>_init() /
 * <sensor>_poll_and_send() pair.
 */
#include <Arduino.h>
#include "board.h"
#include "crsf.h"
{{SENSOR_INCLUDES}}

#define TELEMETRY_INTERVAL_MS 100

void setup() {
    {{CRSF_INIT_CALL}}
{{SENSOR_INIT_CALLS}}
}

void loop() {
{{SENSOR_POLL_CALLS}}
    delay(TELEMETRY_INTERVAL_MS);
}
